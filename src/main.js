// エントリーポイント。必要に応じて JS をここに追加する。
//
// 画像は HTML に <img src="/src/assets/foo.png"> と書くだけでよい（import 不要）。
// ビルド時に autoWebp プラグイン（vite.config.js）が jpg/png を WebP へ自動変換する。

// まだ処理が無い間、空モジュールとして明示する（コードを追加したら削除可）。
export {};
